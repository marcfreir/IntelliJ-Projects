package com.marc.world;

import java.awt.image.BufferedImage;

public class SpaceBackgroundTile extends Tile {

    public SpaceBackgroundTile(int tileAxisX, int tileAxisY, BufferedImage tileSprite) {
        super(tileAxisX, tileAxisY, tileSprite);
    }
}
